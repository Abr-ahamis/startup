# i3blocks config - GNOME-style

separator_block_width=15
# Fonts are configured in i3bar (i3 config). This controls markup parsing only.
markup=pango

[Status]
command=bash ~/.config/i3blocks/scripts/status.sh
interval=persist
